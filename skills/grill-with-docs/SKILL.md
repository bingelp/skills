---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADRs and glossary) as we go. Use when the user wants to stress-test a plan or design before committing to it, outside the normal /spec or /plan flow.
disable-model-invocation: true
---

Run a `grilling` session, using the `domain-modeling` skill.
